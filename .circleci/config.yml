version: 2
jobs:
  build:
    docker:
      - image: docker:20.10-dind
    steps:
      - checkout
      - run: |
          add --no-cache git
          set -xu
          sh .circleci/scripts/build.sh

workflows:
  version: 2
  main:
    jobs:
      - build:
          filters:
            branches:
              only:
                - main
